<UserControl x:Class="Imprelia.PrintAgent.Views.RemoteBridgeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:Imprelia.PrintAgent.Localization"
             Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,6,0">
        <StackPanel Margin="24,20,24,24" MaxWidth="620">

            <!-- Título -->
            <TextBlock Style="{StaticResource T.PageTitle}" Text="{loc:Tr bridge.title}"/>
            <TextBlock Style="{StaticResource T.PageSub}" TextWrapping="Wrap" Text="{loc:Tr bridge.subtitle}"/>

            <!-- ── Estado de conexión ─────────────────────────────── -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Indicador de color -->
                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#6B7280"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                        <Setter Property="Fill" Value="#22C55E"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsConnecting}" Value="True">
                                        <Setter Property="Fill" Value="#F59E0B"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsError}" Value="True">
                                        <Setter Property="Fill" Value="#EF4444"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>

                    <!-- Texto estado -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding ConnectionStateLabel}"
                                   Style="{StaticResource T.Value}" FontWeight="Medium"/>
                        <TextBlock Text="{Binding LastError}"
                                   Foreground="{StaticResource Accent.Red}"
                                   FontSize="11" TextWrapping="Wrap" Margin="0,2,0,0"
                                   Visibility="{Binding LastError, Converter={StaticResource StrToVis}}"/>
                        <TextBlock Foreground="{StaticResource Text.Muted}"
                                   FontSize="11" Margin="0,2,0,0"
                                   Text="{Binding LastConnectedAt, Mode=OneWay, StringFormat='Última conexión: {0}'}"
                                   Visibility="{Binding LastConnectedAt, Converter={StaticResource StrToVis}}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ── Habilitar bridge ───────────────────────────────── -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                <CheckBox IsChecked="{Binding Enabled, Mode=TwoWay}"
                          Style="{StaticResource Input.Check}">
                    <StackPanel>
                        <TextBlock Text="{loc:Tr bridge.enable}"
                                   Foreground="{StaticResource Text.Primary}"
                                   FontSize="13" FontWeight="Medium"/>
                        <TextBlock Text="{loc:Tr bridge.enableSub}"
                                   Style="{StaticResource T.Muted}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                    </StackPanel>
                </CheckBox>
            </Border>

            <!-- ── Campos de configuración ───────────────────────── -->
            <StackPanel IsEnabled="{Binding Enabled}">

                <!-- Server URL -->
                <TextBlock Text="{loc:Tr bridge.serverUrl}" Style="{StaticResource T.Label}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding ServerUrl, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource Input.Text}" Margin="0,0,0,4"/>
                <TextBlock Text="{loc:Tr bridge.serverUrlHint}" Style="{StaticResource T.Muted}" Margin="0,0,0,12"/>

                <!-- Agent ID -->
                <TextBlock Text="{loc:Tr bridge.agentId}" Style="{StaticResource T.Label}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding AgentId, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource Input.Text}" Margin="0,0,0,4"/>
                <TextBlock Text="{loc:Tr bridge.agentIdHint}" Style="{StaticResource T.Muted}" Margin="0,0,0,12"/>

                <!-- API Key (code-behind maneja PasswordBox → ViewModel) -->
                <TextBlock Text="{loc:Tr bridge.apiKey}" Style="{StaticResource T.Label}" Margin="0,0,0,4"/>
                <PasswordBox x:Name="ApiKeyBox"
                             PasswordChanged="ApiKeyBox_PasswordChanged"
                             Background="{StaticResource Bg.Input}"
                             Foreground="{StaticResource Text.Primary}"
                             BorderBrush="{StaticResource Bg.InputBorder}"
                             BorderThickness="1"
                             Padding="10,6"
                             FontSize="13"
                             Margin="0,0,0,12"/>

                <!-- Modo de conexión -->
                <TextBlock Text="{loc:Tr bridge.modeLabel}" Style="{StaticResource T.Label}" Margin="0,0,0,4"/>
                <ComboBox SelectedValue="{Binding Mode, Mode=TwoWay}"
                          SelectedValuePath="Tag"
                          Style="{StaticResource Input.Combo}"
                          Margin="0,0,0,12">
                    <ComboBoxItem Content="{loc:Tr bridge.modeSignalr}" Tag="signalr"/>
                    <ComboBoxItem Content="{loc:Tr bridge.modePolling}" Tag="polling"/>
                </ComboBox>

                <!-- Intervalo polling (solo visible en modo polling) -->
                <StackPanel Margin="0,0,0,12"
                            Visibility="{Binding IsPollingMode, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{loc:Tr bridge.pollingInterval}"
                               Style="{StaticResource T.Label}" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding PollingSeconds, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource Input.Number}"/>
                </StackPanel>

                <!-- Auto-reconexión -->
                <CheckBox IsChecked="{Binding AutoReconnect, Mode=TwoWay}"
                          Content="{loc:Tr bridge.autoReconnectFull}"
                          Style="{StaticResource Input.Check}"
                          Margin="0,0,0,20"/>

            </StackPanel>

            <!-- ── Botones ────────────────────────────────────────── -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <Button Content="{loc:Tr bridge.save}"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource Btn.Primary}"
                        Margin="0,0,10,0"/>
                <Button Command="{Binding TestConnectionCommand}"
                        Style="{StaticResource Btn.Ghost}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{loc:Tr bridge.testConnection}" VerticalAlignment="Center"/>
                        <TextBlock Text=" (…)" VerticalAlignment="Center"
                                   Visibility="{Binding IsTesting, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Resultado del test -->
            <TextBlock Text="{Binding TestResult}"
                       FontSize="12" TextWrapping="Wrap"
                       Visibility="{Binding TestResult, Converter={StaticResource StrToVis}}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{StaticResource Accent.Red}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding TestResultIsSuccess}" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource Accent.Green}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- ── Token para clientes ────────────────────────────── -->
            <Border Style="{StaticResource Card}" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="Token para clientes" Style="{StaticResource T.CardTitle}"/>
                    <TextBlock Style="{StaticResource T.Muted}" TextWrapping="Wrap" Margin="0,2,0,10"
                               Text="Copiá este token y pegalo en cada PC cliente (al elegir rol Cliente). Trae la URL del hub, el AgentId y la API key — no hace falta cargar nada a mano."/>
                    <TextBox Text="{Binding ClientToken, Mode=OneWay}" IsReadOnly="True"
                             Style="{StaticResource Input.Text}" Height="56" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="11"/>
                    <Button Content="📋 Copiar token" Command="{Binding CopyTokenCommand}"
                            Style="{StaticResource Btn.Primary}" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
