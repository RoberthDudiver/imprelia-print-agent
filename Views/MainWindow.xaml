<Window x:Class="Imprelia.PrintAgent.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Imprelia.PrintAgent.ViewModels"
        xmlns:views="clr-namespace:Imprelia.PrintAgent.Views"
        Title="Imprelia Print Agent"
        Width="1200" Height="750"
        MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="#0B1426"
        FontFamily="Segoe UI"
        Foreground="#E2E8F0">

    <Window.Resources>
        <!-- DataTemplate: ViewModel → View mapping -->
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PrintersViewModel}">
            <views:PrintersView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RoutesViewModel}">
            <views:RoutesView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LogsViewModel}">
            <views:LogsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RemoteBridgeViewModel}">
            <views:RemoteBridgeView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="42"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="245"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ══════════════════════════════════════════
             SIDEBAR
        ══════════════════════════════════════════ -->
        <Grid Grid.Row="0" Grid.Column="0" Grid.RowSpan="1"
              Background="#060D1B">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo / App header -->
            <Border Grid.Row="0" Padding="20,18,20,12"
                    BorderBrush="#1E293B" BorderThickness="0,0,0,1">
                <StackPanel Orientation="Horizontal">
                    <Border Width="36" Height="36" CornerRadius="8"
                            Background="#1D3A6E" Margin="0,0,12,0">
                        <TextBlock Text="🖨" FontSize="20" HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Imprelia" FontSize="15" FontWeight="Bold"
                                   Foreground="White"/>
                        <TextBlock Text="Print Agent" FontSize="11"
                                   Foreground="#64748B"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Navigation -->
            <StackPanel Grid.Row="1" Margin="0,12,0,0">
                <!-- Dashboard -->
                <Button Command="{Binding NavDashboardCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavBtn}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedNavIndex}" Value="0">
                                    <Setter Property="Background" Value="#1D3A6E"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⌂" FontSize="16" Width="24" VerticalAlignment="Center"/>
                        <TextBlock Text="Dashboard" VerticalAlignment="Center" Margin="6,0,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Impresoras -->
                <Button Command="{Binding NavPrintersCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavBtn}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedNavIndex}" Value="1">
                                    <Setter Property="Background" Value="#1D3A6E"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🖨" FontSize="16" Width="24" VerticalAlignment="Center"/>
                        <TextBlock Text="Impresoras" VerticalAlignment="Center" Margin="6,0,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Rutas -->
                <Button Command="{Binding NavRoutesCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavBtn}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedNavIndex}" Value="2">
                                    <Setter Property="Background" Value="#1D3A6E"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⇄" FontSize="16" Width="24" VerticalAlignment="Center"/>
                        <TextBlock Text="Rutas" VerticalAlignment="Center" Margin="6,0,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Configuración -->
                <Button Command="{Binding NavSettingsCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavBtn}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedNavIndex}" Value="3">
                                    <Setter Property="Background" Value="#1D3A6E"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚙" FontSize="16" Width="24" VerticalAlignment="Center"/>
                        <TextBlock Text="Configuración" VerticalAlignment="Center" Margin="6,0,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Logs -->
                <Button Command="{Binding NavLogsCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavBtn}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedNavIndex}" Value="4">
                                    <Setter Property="Background" Value="#1D3A6E"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="☰" FontSize="16" Width="24" VerticalAlignment="Center"/>
                        <TextBlock Text="Logs" VerticalAlignment="Center" Margin="6,0,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Remote Bridge -->
                <Button Command="{Binding NavBridgeCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavBtn}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedNavIndex}" Value="5">
                                    <Setter Property="Background" Value="#1D3A6E"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⇡" FontSize="16" Width="24" VerticalAlignment="Center"/>
                        <TextBlock Text="Remote Bridge" VerticalAlignment="Center" Margin="6,0,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Agent status card at bottom of sidebar -->
            <Border Grid.Row="2" Margin="12,0,12,12" Padding="12"
                    Background="#0F172A" CornerRadius="8"
                    BorderBrush="#1E293B" BorderThickness="1">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Ellipse Width="8" Height="8" Fill="#22C55E"
                                 VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Agente activo" FontSize="12"
                                   Foreground="#22C55E" FontWeight="SemiBold"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="Versión  " FontSize="11" Foreground="#64748B"/>
                        <TextBlock Text="{Binding AgentVersion}" FontSize="11" Foreground="#94A3B8"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Puerto  " FontSize="11" Foreground="#64748B"/>
                        <TextBlock Text="{Binding AgentPort}" FontSize="11" Foreground="#94A3B8"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- ══════════════════════════════════════════
             CONTENT AREA
        ══════════════════════════════════════════ -->
        <ContentControl Grid.Row="0" Grid.Column="1"
                        Content="{Binding CurrentPage}"
                        Focusable="False"/>

        <!-- ══════════════════════════════════════════
             FOOTER BAR
        ══════════════════════════════════════════ -->
        <Grid Grid.Row="1" Grid.ColumnSpan="2"
              Background="#060D1B">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left: security info -->
            <StackPanel Grid.Column="0" Orientation="Horizontal"
                        VerticalAlignment="Center" Margin="20,0">
                <TextBlock Text="🔒" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBlock Text="Seguro y local" FontSize="12" Foreground="#22C55E"
                           VerticalAlignment="Center" Margin="0,0,16,0" FontWeight="SemiBold"/>
                <Ellipse Width="4" Height="4" Fill="#334155" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <TextBlock FontSize="12" Foreground="#64748B" VerticalAlignment="Center">
                    El agente escucha en
                    <Run Text="{Binding ListeningAddress, Mode=OneWay}" Foreground="#60A5FA"/>
                    por defecto
                </TextBlock>
            </StackPanel>

            <!-- Right: actions -->
            <StackPanel Grid.Column="1" Orientation="Horizontal"
                        VerticalAlignment="Center" Margin="0,0,16,0">
                <Button Content="📖 Guía API" Command="{Binding OpenApiGuideCommand}"
                        Style="{StaticResource Btn.Ghost}"
                        Height="28" Padding="10,0" FontSize="12" Margin="0,0,8,0"/>
                <Button Content="⊟ Minimizar" Command="{Binding MinimizeCommand}"
                        Style="{StaticResource Btn.Ghost}"
                        Height="28" Padding="10,0" FontSize="12" Margin="0,0,8,0"/>
                <Button Content="⏻ Salir" Command="{Binding ExitCommand}"
                        Style="{StaticResource Btn.Danger}"
                        Height="28" Padding="10,0" FontSize="12"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
